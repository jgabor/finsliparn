pre-commit:
  parallel: true
  commands:
    typescript:
      run: bunx ultracite fix
      stage_fixed: true
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
    markdown:
      run: bunx markdownlint --dot --fix --disable MD013 MD029 MD033 MD036 MD040 --
      stage_fixed: true
      glob: "*.md"
    configs:
      stage_fixed: true
      glob: "*.{json,yml,yaml}"
      run: "bunx prettier --write {staged_files}"

pre-push:
  parallel: true
  commands:
    check:
      run: "bunx ultracite check"
    typecheck:
      run: "bunx tsc --noEmit"
    test:
      run: "bun test"
